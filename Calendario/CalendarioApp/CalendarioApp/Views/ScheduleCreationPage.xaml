<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CalendarioApp.Views.ScheduleCreationPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:CalendarioApp.Controls"
    xmlns:local="clr-namespace:CalendarioApp.ViewModels"
    Title="Planowanie wydarzenia"
    BackgroundColor="{Binding PageBackground}">
    <ContentPage.BindingContext>
        <local:ScheduleCreationPageViewModel />
    </ContentPage.BindingContext>

    <Frame
        Margin="20"
        Padding="20"
        BackgroundColor="{Binding FrameBackground}"
        CornerRadius="30"
        HorizontalOptions="Fill"
        VerticalOptions="Center">
        <StackLayout>
            <Label
                FontSize="24"
                HorizontalOptions="Center"
                Text="Zaplanuj wydarzenie"
                TextColor="{Binding PrimaryColor}" />

            <StackLayout Margin="0,10">
                <Label
                    FontSize="16"
                    Text="Zadanie"
                    TextColor="{Binding PrimaryColor}" />
                <Picker
                    x:Name="TaskPicker"
                    Title="Wybierz zadanie"
                    ItemsSource="{Binding Tasks}"
                    ItemDisplayBinding="{Binding Name}"/>
            </StackLayout>

            <StackLayout Margin="0,10" Orientation="Horizontal">
                <CheckBox x:Name="IsScheduleAllDay"/>
                <Label
                    Margin="0, 5, 0, 0"
                    FontSize="16"
                    Text="Wydarzenie trwa cały dzień?"
                    TextColor="{Binding PrimaryColor}" />
                <StackLayout.GestureRecognizers>
                    <TapGestureRecognizer Tapped="IsScheduleAllDayTapped" />
                </StackLayout.GestureRecognizers>
            </StackLayout>

            <StackLayout Margin="0,10" x:Name="ScheduleStart">
                <Label
                    FontSize="16"
                    Text="Rozpoczęcie wydarzenia"
                    TextColor="{Binding PrimaryColor}" />
                <TimePicker x:Name="ScheduleStartTimePicker" Time="{Binding TimeNow}" />
            </StackLayout>

            <StackLayout Margin="0,10" x:Name="ScheduleEnd">
                <Label
                    FontSize="16"
                    Text="Zakończenie wydarzenia"
                    TextColor="{Binding PrimaryColor}" />
                <TimePicker x:Name="ScheduleEndTimePicker" Time="{Binding TimeNow}" />
            </StackLayout>

            <Button
                x:Name="CreateSchedule"
                BackgroundColor="{Binding PrimaryColor}"
                Clicked="CreateScheduleClicked"
                CornerRadius="30"
                HorizontalOptions="Center"
                Text="Zaplanuj wydarzenie"
                TextColor="{Binding SecondaryColor}"
                TextTransform="None"
                VerticalOptions="EndAndExpand" />
        </StackLayout>
    </Frame>
</ContentPage>